{% extends 'admin/base.html.twig' %}

{% block title %}Ajouter un Quizz {{ parent() }}{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-lg-12">
            <h1>Ajouter un nouveau Quizz</h1>

            {{ include('admin/quizz/_form.html.twig') }}

            <a href="{{ path('admin_quizz_index') }}" class="btn btn-xs btn-primary m-t-lg"><< Retour</a>
        </div>
    </div>
{% endblock %}
