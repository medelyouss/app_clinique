{% extends 'admin/base.html.twig' %}

{% block title %}Modifier {{ quizz.designation }} {{ parent() }}{% endblock %}

{% block body %}
    <h1>Modifier {{ quizz.designation }}</h1>

    {{ include('admin/quizz/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('admin_quizz_index') }}" class="btn btn-xs btn-primary m-t-lg"><< Retour</a>

    <span class="pull-right">{{ include('admin/quizz/_delete_form.html.twig') }}</span>
{% endblock %}
