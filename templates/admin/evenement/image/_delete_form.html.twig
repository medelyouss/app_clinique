<form method="post" action="{{ path('admin_evenementimage_delete', {'id': evenement_image.id}) }}"
      onsubmit="return confirm('Action irreversible, Continuer?');" style="display: inline;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evenement_image.id) }}">
    <button class="btn btn-xs btn-danger btn-circle"><i class="fa fa-trash"></i> </button>
</form>
