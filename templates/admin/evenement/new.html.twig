{% extends 'admin/base.html.twig' %}

{% block title %}Ajouter Evenement {{ parent() }}{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-lg-12">
            <h1>Ajouter evenement</h1>

            {{ include('admin/evenement/_form.html.twig') }}

            <a href="{{ path('admin_evenement_index') }}" class="btn btn-xs btn-primary m-t-lg"><< Retour</a>
        </div>
    </div>
{% endblock %}
