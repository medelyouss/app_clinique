{% extends 'admin/base.html.twig' %}

{% block title %}Discipline{{ parent() }}{% endblock %}

{% block body %}
    <h1>Discipline</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ discipline.id }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ discipline.designation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_discipline_index') }}"><< Retour</a>

    <a href="{{ path('admin_discipline_edit', {'id': discipline.id}) }}">Modifier</a>

    <span class="pull-right">{{ include('admin/discipline/_delete_form.html.twig') }}</span>
{% endblock %}
