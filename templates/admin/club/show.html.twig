{% extends 'admin/base.html.twig' %}

{% block title %}Club{% endblock %}

{% block body %}
    <h1>Club</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ club.id }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ club.designation }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ club.codeClub }}</td>
            </tr>
            <tr>
                <th>Numtel</th>
                <td>{{ club.numtel }}</td>
            </tr>
            <tr>
                <th>Responsable</th>
                <td>{{ club.responsable }}</td>
            </tr>
            <tr>
                <th>Île</th>
                <td>{{ club.ile }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_club_index') }}" class="btn btn-xs btn-primary"><< Retour</a>

    <a href="{{ path('admin_club_edit', {'id': club.id}) }}" class="btn btn-xs btn-warning">Modifier</a>

    {{ include('admin/club/_delete_form.html.twig') }}
{% endblock %}
