{% extends 'admin/base.html.twig' %}

{% block title %}Ajouter Actu sport {{ parent() }}{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-lg-12">
            <h1>Ajouter Actu sport</h1>

            {{ include('admin/actusport/_form.html.twig') }}

            <a href="{{ path('admin_actusport_index') }}" class="btn btn-xs btn-primary m-t-lg"><< Retour</a>
        </div>
    </div>
{% endblock %}
