{% extends 'admin/base.html.twig' %}

{% block title %}Ajouter Image {{ parent() }}{% endblock %}

{% block body %}
   <div class="row">
       <div class="col-lg-12">
           <h1>Ajouter une image</h1>

           {{ include('admin/actusport/image/_form.html.twig') }}

           <a href="{{ path('admin_actusport_show', {id : actusport_id}) }}" class="btn btn-xs btn-primary m-t-lg"><< Retour</a>
       </div>
   </div>
{% endblock %}
