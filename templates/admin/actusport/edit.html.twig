{% extends 'admin/base.html.twig' %}

{% block title %}Modifier Actu sport {{ parent() }}{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-lg-12">
            <h1>Modifier Actu sport</h1>

            {{ include('admin/actusport/_form.html.twig', {'button_label': 'Modifier'}) }}

            <a href="{{ path('admin_actusport_index') }}" class="btn btn-xs btn-primary m-t-lg"><< Retour</a>

            <span class="pull-right">{{ include('admin/actusport/_delete_form.html.twig') }}</span>
        </div>
    </div>
{% endblock %}
