{% extends 'admin/base.html.twig' %}

{% block title %}Modifier Quizz Category {{ parent() }}{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-lg-12">
            <h1>Modifier Quizz Categorie</h1>

            {{ include('admin/quizz_category/_form.html.twig', {'button_label': 'Modifier'}) }}

            <a href="{{ path('admin_quizzcategory_index') }}" class="btn btn-primary btn-xs"><< Retour</a>

            <span class="pull-right">{{ include('admin/quizz_category/_delete_form.html.twig') }}</span>
        </div>
    </div>
{% endblock %}
