{% extends 'admin/base.html.twig' %}

{% block title %}Ajouter actu flash {{ parent() }}{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-lg-12">
            <h1>Nouvelle actu flash</h1>

            {{ include('admin/infoflash/_form.html.twig') }}

            <a href="{{ path('admin_infoflash_index') }}" class="btn btn-xs btn-primary m-t-lg"><< Retour</a>
        </div>
    </div>
{% endblock %}
