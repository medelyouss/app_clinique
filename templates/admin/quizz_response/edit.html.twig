{% extends 'admin/base.html.twig' %}

{% block title %}Modifier Quizz Response {{ parent() }}{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-lg-12">
            <h1>Modifier Quizz Response</h1>

            {{ include('admin/quizz_response/_form.html.twig', {'button_label': 'Modifier'}) }}

            <a href="{{ path('admin_quizzquestion_show', {id:quizz_response.quizzQuestion.id}) }}" class="btn btn-primary btn-xs m-t-lg"><< Retour</a>

            <span class="pull-right">
                {{ include('admin/quizz_response/_delete_form.html.twig') }}
            </span>

        </div>
    </div>
{% endblock %}
