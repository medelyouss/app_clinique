{% extends 'admin/base.html.twig' %}

{% block title %}Ile{% endblock %}

{% block body %}
    <h1>Ile</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ile.id }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ ile.designation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_ile_index') }}" class="btn btn-xs btn-primary"><< Retour à la liste</a>



    <span class="pull-right">
        <a href="{{ path('admin_ile_edit', {'id': ile.id}) }}" class="btn btn-outline btn-warning btn-xs    ">
            <i class="fa fa-edit"></i> Modifier
        </a>
        {{ include('admin/ile/_delete_form.html.twig') }}
    </span>
{% endblock %}
