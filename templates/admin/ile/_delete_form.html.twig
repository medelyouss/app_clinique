<form method="post" action="{{ path('admin_ile_delete', {'id': ile.id}) }}"
      onsubmit="return confirm('Are you sure you want to delete this item?');" style="display: inline">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ile.id) }}">
    <button class="btn btn-danger btn-xs"><i class="fa fa-trash"></i> Supprimer</button>
</form>
