{% extends 'admin/base.html.twig' %}

{% block title %}Edit Quizz Question {{ parent() }}{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-lg-12">
            <h1>Edit Quizz Question</h1>

            {{ include('admin/quizz_question/_form.html.twig', {'button_label': 'Modifier'}) }}

            <a href="{{ path('admin_quizzquestion_index') }}" class="btn btn-xs btn-primary m-t-lg"><< Retour</a>

            <span class="pull-right">{{ include('admin/quizz_question/_delete_form.html.twig') }}</span>
        </div>
    </div>
{% endblock %}
