{% extends 'admin/base.html.twig' %}

{% block title %}Modifier Reglement {{ parent() }}{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-lg-12">
            <h1>Modifier Reglement</h1>

            {{ include('admin/reglement/_form.html.twig', {'button_label': 'Appliquer les modifications'}) }}

            <a href="{{ path('admin_reglement_index') }}" class="btn btn-xs btn-primary m-t-lg"><< Retour</a>

            <span class="pull-right">{{ include('admin/reglement/_delete_form.html.twig') }}</span>
        </div>
    </div>
{% endblock %}
